app-id: io.github.ellie_commons.badger

runtime: io.elementary.Platform
runtime-version: '8'
sdk: io.elementary.Sdk

command: io.github.ellie_commons.badger

finish-args:
  - '--share=ipc'
  - '--device=dri'
  - '--socket=fallback-x11'
  - '--socket=wayland'
  - '--socket=session-bus'

modules:
  - name: badger
    buildsystem: meson
    sources:
      - type: dir
        path: .
